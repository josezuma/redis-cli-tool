# redis-cli-tool

Redis CLI helper with pretty output.

## For AI agents
- scripts/redis_tool.py wraps redis-cli with formatted output
- Commands: info, keys, memory, slowlog, clients, ping
- Use --json for machine-readable output
- Supports --host and --port for remote connections
